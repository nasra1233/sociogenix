name: sociogenix
description: SocioGenix Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  charts_flutter: ^0.12.0
  flutter_stripe: ^9.0.0
  firebase_core: ^2.4.0
  cloud_firestore: ^4.3.0
  firebase_auth: ^4.2.0
  firebase_storage: ^11.0.0
  url_launcher: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
